{% extends 'base.html.twig' %}

{% block title %}Kaamel'O'tt - Connexion{% endblock %}

{% block body %}
{# <form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
    
        <div class="mb-3 login_on">
            <p>Vous êtes connecté en tant que {{ app.user.userIdentifier }}  ,   <a href="{{ path('app_logout') }}" class="deconnection">Déconnexion</a></p>
        </div>
    {% else %}

    <div class="login_position">
    <div class="login_size">
    <h1 class="h3 mb-3 font-weight-normal">Veuillez vous connecter</h1>
    <label for="inputEmail">Email</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
    <label for="inputPassword">Mot de passe</label>
    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
    > #}

   {#  <button class="btn btn-lg btn-secondary loginBtn" type="submit">
        Valider
    </button>

    </div>
    </div>
 #}
    {# {% endif %} #}


    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}

    
    {# <button class="btn btn-lg btn-primary" type="submit">
        Valider
    </button>
</form> #}


<form method="post">
<main  class="p-3">
    <div class="text-center mb-3">
    
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
    
        <div class="mb-3">
            Vous êtes connecté en tant que {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Déconnexion</a>
        </div>
    {% else %}
    <h2 class="main__item">Veuillez vous connecter</h2>
  
    <div class="card mt-3 mb-3"> 
        <div class="card-body text-bg-secondary">
            {# {{ form_start(formulaire, {'attr': {'novalidate': 'novalidate'}}) }} #}
        <label for="inputEmail">Email</label>
        <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
        <label for="inputPassword">Mot de passe</label>
        <input type="password" name="password" id="inputPassword" class="form-control mb-3" autocomplete="current-password" required>

        <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}">
       
            {# {{ form_row(formulaire.rating)}}
   
        <button type="submit" class="btn btn-outline-light" id="button_add_review">Ajouter la Note</button>
            {{ form_end(formulaire) }} #}
            <button class="btn btn-lg btn-outline-light" type="submit">
        Valider
    </button>
        </div>
    </div>
  {% endif %}
</main>

</form>

{% endblock %}
