<form method="post" action="{{ path('app_backoffice_personage_delete', {'id': personage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personage.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
