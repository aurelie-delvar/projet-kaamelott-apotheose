{% extends 'base.html.twig' %}

{% block title %}Personage{% endblock %}

{% block body %}
    <h1>Personage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ personage.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ personage.picture }}</td>
            </tr>
            <tr>
                <th>DescriptionPicture</th>
                <td>{{ personage.descriptionPicture }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ personage.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backoffice_personage_index') }}">back to list</a>

    <a href="{{ path('app_backoffice_personage_edit', {'id': personage.id}) }}">edit</a>

    {{ include('backoffice/personage/_delete_form.html.twig') }}
{% endblock %}
