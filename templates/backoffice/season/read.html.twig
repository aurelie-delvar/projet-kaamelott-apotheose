{% extends 'base.html.twig' %}

{% block title %}Season Browse{% endblock %}

{% block body %}

<main>
    
    <div class="main-content">
        <h1>SAISON {{ season.id }}</h1>
    </div>

    <div class="container  ">
        <a href="{{ path('app_backoffice_season_browse') }}" class="btn btn-primary">Retour à la liste</a>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Titre</th>
                    <th scope="col">actions</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <th scope="row">{{ season.id }}</th>
                    <td>{{ season.title }}</td>
                    <td>
                        <a href="{{ path('app_backoffice_season_edit', {'id': season.id}) }}" class="btn btn-warning">Modifier</a>
                        <a href="{{ path('app_backoffice_season_delete', {'id': season.id}) }}" class="btn btn-danger">Supprimer</a>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</main>    
{% endblock %}
