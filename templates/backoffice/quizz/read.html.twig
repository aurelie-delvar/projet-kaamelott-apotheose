{% extends 'base.html.twig' %}

{% block title %}Kaamel'O'tt - Quizz{% endblock %}

{% block body %}
<main>

    <div class="main-content">
        <h1>Fiche de quizz</h1>
    </div>

    <div class="container">

    <a href="{{ path('app_backoffice_quizz_browse') }}" class="btn btn-primary">Retour à la liste</a>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quizz.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ quizz.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backoffice_quizz_edit', {'id': quizz.id}) }}" class="btn btn-warning">Modifier</a>

    {{ include('backoffice/quizz/_delete_form.html.twig') }}

    </div>

</main>
{% endblock %}
