{% extends 'base.html.twig' %}

{% block title %}Episode{% endblock %}

{% block body %}
    <h1>Episode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ episode.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ episode.title }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ episode.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backoffice_episode_browse') }}">back to list</a>

    <a href="{{ path('app_backoffice_episode_edit', {'id': episode.id}) }}">edit</a>

    {{ include('backoffice/episode/_delete_form.html.twig') }}
{% endblock %}
