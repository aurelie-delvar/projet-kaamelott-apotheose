{% extends 'base.html.twig' %}

{% block title %}Kaamel'O'tt - Ajouter une citation{% endblock %}

{% block body %}
<main  class="p-3">
    <div class="text-center mb-3">
        
        <h2 class="main__item ">Proposer une citation</h2>
    
        <div class="card mt-3 mb-3"> 
            <div class="card-body text-bg-secondary">
                <a href="{{ path('default') }}" class="btn btn-outline-light addQuoteBtn mb-3">Retour à l'accueil</a>
                {{ form_start(form) }}

                    {{ form_widget(form) }}
                    <button class="btn btn-outline-light">{{ button_label|default('Valider') }}</button>

                {{ form_end(form) }}
        </div>
    </div>
</main>  
{% endblock %}